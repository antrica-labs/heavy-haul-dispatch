<Window x:Class="SingerDispatch.Windows.DocumentViewerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Document Viewer" Height="700" Width="900"
    Loaded="Window_Loaded">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SingerDispatchDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <DockPanel>
        <Border DockPanel.Dock="Top" BorderBrush="LightGray" BorderThickness="0,0,0,1" Background="WhiteSmoke">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />                    
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                                
                <Button Grid.Column="1" Margin="4" Click="PDF_Click">Save to PDF</Button>
                <CheckBox Grid.Column="2" HorizontalAlignment="Right" Margin="0,7,6,-1" Name="IsMetricCB" Checked="ApplyMetric_Checked" Unchecked="ApplyMetric_Unchecked" IsChecked="True">Metric units</CheckBox>
            </Grid>
        </Border>

        <WebBrowser Grid.Row="1" Name="TheBrowser" />
    </DockPanel>
</Window>
